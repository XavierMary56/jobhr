# TG HR Platform - 前端项目

简化版 Next.js 前端应用，用于候选人管理和浏览。

## 📁 项目结构

```
frontend/
├── app/                          # Next.js 页面路由
│   ├── layout.tsx                # 根布局（含 Toast）
│   ├── page.tsx                  # 登录页
│   ├── candidates/               # 候选人相关页面
│   │   ├── page.tsx              # 候选人列表
│   │   └── [slug]/page.tsx       # 候选人详情
│   ├── audit-logs/page.tsx       # 审计日志页
│   └── waiting-approval/page.tsx # 等待审批页
├── components/                   # 可复用组件
│   ├── Header.tsx                # 顶部导航栏
│   ├── FilterBar.tsx             # 筛选搜索栏
│   └── CandidateCard.tsx         # 候选人卡片
├── lib/                          # 核心库文件
│   ├── api.ts                    # API 客户端（完整注释）
│   ├── store.ts                  # 全局状态管理（Zustand）
│   ├── constants.ts              # 常量配置（NEW）
│   └── utils.ts                  # 工具函数（NEW）
├── styles/
│   └── globals.css               # 全局样式 + Tailwind
├── package.json                  # 依赖配置
├── tsconfig.json                 # TypeScript 配置
├── next.config.js                # Next.js 配置
└── Dockerfile                    # Docker 镜像配置
```

## 🎯 核心优化

### 1. **代码组织优化**
- ✅ 提取常量到 `constants.ts`（英语水平、技能列表、路由等）
- ✅ 创建 `utils.ts` 工具函数库（格式化、验证等）
- ✅ API 文件添加完整 JSDoc 注释
- ✅ 组件添加功能说明注释

### 2. **可读性提升**
- ✅ 所有接口和类型添加详细注释
- ✅ 函数添加用途和参数说明
- ✅ 组件逻辑清晰分块（基础信息、详细信息、技能标签等）
- ✅ 使用语义化的变量和函数名

### 3. **可维护性改进**
- ✅ 集中管理配置常量，避免硬编码
- ✅ 抽取通用工具函数，避免重复代码
- ✅ 统一错误处理和消息显示
- ✅ 一致的代码风格和结构

## 🚀 快速开始

### 本地开发

```bash
# 安装依赖
npm install

# 配置环境变量
echo "NEXT_PUBLIC_API_URL=http://localhost:8080" > .env.local

# 启动开发服务器
npm run dev

# 访问 http://localhost:3000
```

### Docker 开发

```bash
# 构建并启动
docker-compose up --build

# 访问 http://localhost:3000
```

## 📚 核心文件说明

### `lib/constants.ts` - 常量配置
集中管理所有硬编码的常量值：
- API 配置（BASE_URL, TIMEOUT）
- 英语水平选项
- 技能列表
- 分页配置
- 审计日志操作映射
- 路由路径

**用途**：避免在代码中硬编码，方便统一修改和维护。

### `lib/utils.ts` - 工具函数
提供跨组件使用的通用辅助函数：
- `formatSalaryRange()` - 格式化薪资范围
- `formatDateTime()` - 格式化日期时间
- `getEnglishLevelLabel()` - 获取英语水平标签
- `getErrorMessage()` - 统一错误消息提取
- 等等...

**用途**：减少重复代码，提高代码复用性。

### `lib/api.ts` - API 客户端
所有后端 API 调用的封装：
- 完整的 TypeScript 类型定义
- 详细的 JSDoc 注释
- 请求/响应拦截器
- 自动处理认证失败

**用途**：统一 API 调用方式，提供类型安全。

### `lib/store.ts` - 全局状态
使用 Zustand 管理用户认证状态：
- 用户信息存储
- 登录/登出方法
- 状态持久化

**用途**：跨组件共享用户状态。

## 🎨 样式系统

使用 **Tailwind CSS** + 自定义工具类：

### 全局样式类（`globals.css`）

```css
.btn-primary      /* 主要按钮 */
.btn-secondary    /* 次要按钮 */
.input-field      /* 输入框 */
.card             /* 卡片容器 */
.badge            /* 标签徽章 */
```

### 使用示例

```tsx
<button className="btn-primary">
  搜索
</button>

<div className="card">
  内容区域
</div>
```

## 🔧 开发建议

### 1. 添加新常量
在 `lib/constants.ts` 中添加，避免硬编码：
```typescript
export const MY_CONFIG = {
  VALUE: 'something',
} as const
```

### 2. 添加新工具函数
在 `lib/utils.ts` 中添加，并加上 JSDoc 注释：
```typescript
/**
 * 功能说明
 * @param param 参数说明
 * @returns 返回值说明
 */
export function myUtil(param: string): string {
  // 实现
}
```

### 3. 添加新 API 方法
在 `lib/api.ts` 中添加：
```typescript
export const myAPI = {
  /**
   * 方法说明
   */
  myMethod: async (): Promise<ResponseType> => {
    const response = await apiClient.get('/api/my-endpoint')
    return response.data
  },
}
```

### 4. 使用工具函数
优先使用 `lib/utils.ts` 中的函数：
```typescript
import { formatSalaryRange } from '@/lib/utils'

// ✅ 推荐
const salary = formatSalaryRange(min, max)

// ❌ 不推荐（硬编码重复逻辑）
const salary = `¥${min} - ¥${max}`
```

## 📦 依赖说明

| 依赖 | 版本 | 用途 |
|------|------|------|
| Next.js | 14.1.0 | React 框架 |
| React | 18.2.0 | UI 库 |
| TypeScript | 5.3.3 | 类型安全 |
| Tailwind CSS | 3.4.1 | 样式框架 |
| Axios | 1.6.5 | HTTP 客户端 |
| Zustand | 4.4.1 | 状态管理 |
| React-Toastify | 9.1.3 | 消息提示 |

## ⚡ 性能优化

- ✅ 使用 Next.js 自动代码分割
- ✅ 按路由懒加载组件
- ✅ 图片自动优化（Next.js Image）
- ✅ API 响应缓存（浏览器缓存）

## 🐛 调试

```bash
# 查看构建输出
npm run build

# 检查 TypeScript 错误
npx tsc --noEmit

# 查看 Tailwind 配置
npx tailwindcss -o output.css
```

## 📝 代码规范

- ✅ 使用 TypeScript 严格模式
- ✅ 所有函数添加 JSDoc 注释
- ✅ 组件添加功能说明注释
- ✅ 优先使用工具函数，避免重复
- ✅ 常量统一管理，避免硬编码
- ✅ 使用语义化命名

## 🔗 相关文档

- [Next.js 官方文档](https://nextjs.org/docs)
- [Tailwind CSS 文档](https://tailwindcss.com/docs)
- [Zustand 文档](https://github.com/pmndrs/zustand)
- [后端 API 文档](../docs/API.md)

---

**简化原则**：代码越简单越好，注释越清楚越好，结构越清晰越好！
